// The release this binary was built as. The committed file says `dev`;
// scripts/common.sh writes the release's version here for `dart build cli`,
// which takes no `--define`, and puts this file back afterwards.
const buildVersion = 'dev';
